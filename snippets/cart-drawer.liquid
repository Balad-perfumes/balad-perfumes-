{%- comment -%}
  Lightweight cart drawer helper. The theme already renders the main cart drawer
  as a section in layout/theme.liquid, so this snippet exists as a reusable
  wrapper for collection/product templates that need the drawer trigger styles
  or an inline render target.
{%- endcomment -%}

<div class="cart-drawer-snippet" aria-hidden="true"></div>

