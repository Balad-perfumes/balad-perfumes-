{% liquid
  assign coll_handle = section.settings.collection
  if coll_handle != blank
    assign active_collection = collections[coll_handle]
  endif
  if active_collection == blank
    assign active_collection = collections['new-in']
  endif
  if active_collection == blank
    assign active_collection = collections['all']
  endif
  assign sizes_list = ''
%}

<section
  class="stagmenfashion-latest color-{{ section.settings.color_scheme }}"
  style="--latest-bg: {{ section.settings.background | default: '#ffffff' }};"
>
  <div class="stagmenfashion-latest__shell">
    <div class="stagmenfashion-latest__head">
      <div class="stagmenfashion-latest__heading-wrap">
        {% if section.settings.heading != blank %}
          <h2 class="stagmenfashion-latest__heading">
            {% assign heading_upper = section.settings.heading | upcase %}
            {% if heading_upper contains 'OUR BESTSELLERS' %}
              DISCOVER <span class="stagmenfashion-latest__highlight">OUR BESTSELLERS</span>
            {% elsif heading_upper contains 'BESTSELLERS' %}
              DISCOVER <span class="stagmenfashion-latest__highlight">OUR BESTSELLERS</span>
            {% elsif heading_upper contains 'NEW ARRIVALS' %}
              NEW <span class="stagmenfashion-latest__highlight">ARRIVALS</span>
            {% else %}
              {{ section.settings.heading }}
            {% endif %}
          </h2>
        {% endif %}
        {% if section.settings.subheading != blank %}
          <p class="stagmenfashion-latest__subheading">{{ section.settings.subheading }}</p>
        {% endif %}
      </div>

      <div class="stagmenfashion-latest__head-right">
        {% assign discover_url = section.settings.discover_more_link | default: active_collection.url %}
        {% if section.settings.discover_more_label != blank and discover_url != blank %}
          <a href="{{ discover_url }}" class="stagmenfashion-latest__discover-more">
            {{ section.settings.discover_more_label }}
          </a>
        {% endif %}
        <div class="stagmenfashion-latest__nav-arrows">
          <button type="button" class="stagmenfashion-latest__nav-arrow stagmenfashion-latest__nav-arrow--prev" data-slider-prev aria-label="Previous products">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="15 18 9 12 15 6"></polyline>
            </svg>
          </button>
          <button type="button" class="stagmenfashion-latest__nav-arrow stagmenfashion-latest__nav-arrow--next" data-slider-next aria-label="Next products">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="9 18 15 12 9 6"></polyline>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <div class="stagmenfashion-latest-grid" data-slider-track>
      {% for product in active_collection.products limit: 12 %}
        {% liquid
          assign images = product.images
          assign image_count = images.size
          assign first_image = product.featured_image | default: images.first
          assign sizes_list = ''
          for opt in product.options_with_values
            assign opt_name_down = opt.name | downcase
            if opt_name_down contains 'size' or opt.name == 'Size'
              assign sizes_list = opt.values | join: ','
            endif
          endfor
          if sizes_list == ''
            assign sizes_check = '|'
            for v in product.variants
              assign sz = v.option1 | strip
              unless sz == blank or sz == 'Default Title'
                assign check_sz = '|' | append: sz | append: '|'
                unless sizes_check contains check_sz
                  assign sizes_check = sizes_check | append: check_sz
                  if sizes_list != ''
                    assign sizes_list = sizes_list | append: ','
                  endif
                  assign sizes_list = sizes_list | append: sz
                endunless
              endunless
            endfor
          endif

          assign size_array = sizes_list | split: ','

          assign fragrance_descriptor = ''
          assign tags_string = product.tags | join: ' ' | downcase
          if tags_string contains 'woody'
            assign fragrance_descriptor = 'WOODY | ALLDAY | UNISEX'
          elsif tags_string contains 'floral'
            assign fragrance_descriptor = 'FLORAL | ALLDAY | UNISEX'
          elsif tags_string contains 'fresh'
            assign fragrance_descriptor = 'FRESH | ALLDAY | MEN'
          elsif tags_string contains 'oriental'
            assign fragrance_descriptor = 'ORIENTAL | NIGHT | MEN'
          elsif tags_string contains 'fruity'
            assign fragrance_descriptor = 'FRUITY | ALLDAY | UNISEX'
          else
            assign fragrance_descriptor = product.vendor | default: 'EAU DE PARFUM' | upcase
          endif
        %}
        <div class="stagmenfashion-latest-grid__card" data-product-card>
          <a href="{{ product.url }}" class="stagmenfashion-latest-grid__media" data-carousel>
            {% if image_count > 0 %}
              {% for image in images limit: 6 %}
                <img
                  class="stagmenfashion-latest-grid__image{% if forloop.first %} is-active{% endif %}"
                  src="{{ image | image_url: width: 500 }}"
                  srcset="{{ image | image_url: width: 300 }} 300w, {{ image | image_url: width: 500 }} 500w"
                  sizes="(max-width: 749px) 80vw, (max-width: 989px) 45vw, 32vw"
                  alt="{{ image.alt | default: product.title | escape }}"
                  width="500"
                  height="500"
                  loading="lazy"
                  decoding="async"
                  data-carousel-slide
                >
              {% endfor %}
            {% else %}
              {{ 'product-1' | placeholder_svg_tag: 'stagmenfashion-latest-grid__placeholder' }}
            {% endif %}

            {% if product.tags contains 'selling-fast' or product.tags contains 'bestseller' or forloop.first %}
              <div class="stagmenfashion-card-badge">
                <span class="stagmenfashion-card-badge__icon">⚡</span> Selling Fast
              </div>
            {% endif %}

            <!-- Heart / Bookmark button -->
            <button
              type="button"
              class="stagmenfashion-latest-grid__heart"
              data-wishlist-heart
              data-product-id="{{ product.id }}"
              data-product-title="{{ product.title }}"
              data-product-price="{{ product.price | money_without_currency | remove: ',' }}"
              data-product-compare-price="{{ product.compare_at_price | money_without_currency | remove: ',' }}"
              data-product-url="{{ product.url }}"
              data-product-image="{{ first_image | image_url: width: 400 }}"
              data-product-sizes="{{ sizes_list }}"
              data-product-available="{{ product.available }}"
              aria-label="Add {{ product.title }} to wishlist"
            >
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round" xmlns="http://www.w3.org/2000/svg">
                <path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"/>
              </svg>
            </button>

            {% if image_count > 1 %}
              <div class="stagmenfashion-latest-grid__dots" data-carousel-dots>
                {% for image in images limit: 6 %}
                  <span class="stagmenfashion-latest-grid__dot{% if forloop.first %} is-active{% endif %}" data-carousel-dot data-index="{{ forloop.index0 }}"></span>
                {% endfor %}
              </div>
            {% endif %}
          </a>

          <div class="stagmenfashion-latest-grid__info">
            <a href="{{ product.url }}" class="stagmenfashion-latest-grid__title-link">
              <h3 class="stagmenfashion-latest-grid__title">{{ product.title }}</h3>
            </a>
            <p class="stagmenfashion-latest-grid__subtitle">{{ fragrance_descriptor }}</p>
            <div class="stagmenfashion-latest-grid__price-wrap">
              <span class="stagmenfashion-latest-grid__price">₹{{ product.price | money_without_currency }}</span>
            </div>

            {% if size_array.size > 0 %}
              <div class="stagmenfashion-latest-grid__size-pills" data-card-size-pills>
                {% for sz in size_array %}
                  {% assign clean_sz = sz | strip %}
                  {% if clean_sz != blank %}
                    <button
                      type="button"
                      class="stagmenfashion-latest-grid__size-pill{% if forloop.first %} is-selected{% endif %}"
                      data-size-value="{{ clean_sz }}"
                    >
                      {{ clean_sz }}
                    </button>
                  {% endif %}
                {% endfor %}
              </div>
            {% endif %}

            <button
              type="button"
              class="stagmenfashion-latest-grid__quick-add-btn"
              data-quick-add
              data-qa-title="{{ product.title }}"
              data-qa-price="{{ product.price | money_without_currency | remove: ',' }}"
              data-qa-url="{{ product.url }}"
              data-qa-id="{{ product.id }}"
              data-qa-image="{{ first_image | image_url: width: 400 }}"
              data-qa-sizes="{{ sizes_list }}"
              data-qa-first-variant="{{ product.selected_or_first_available_variant.id }}"
              data-qa-variants="{% for v in product.variants %}{% unless v.option1 == 'Default Title' %}{{ v.option1 }}:{{ v.id }},{% endunless %}{% endfor %}"
              data-qa-available="{{ product.available }}"
              data-qa-variant-availability="{% for v in product.variants %}{% unless v.option1 == 'Default Title' %}{{ v.option1 }}:{{ v.available }},{% endunless %}{% endfor %}"
              aria-label="Quick add {{ product.title }}"
            >
              Quick Add
            </button>
          </div>
        </div>
      {% endfor %}

      {% if active_collection.products.size == 0 %}
        {% for i in (1..6) %}
          <div class="stagmenfashion-latest-grid__card stagmenfashion-latest-grid__card--placeholder">
            <div class="stagmenfashion-latest-grid__media">
              {{ 'product-1' | placeholder_svg_tag: 'stagmenfashion-latest-grid__placeholder' }}
            </div>
            <div class="stagmenfashion-latest-grid__info">
              <h3 class="stagmenfashion-latest-grid__title">Product {{ i }}</h3>
              <p class="stagmenfashion-latest-grid__subtitle">FRAGRANCE NOTES</p>
              <div class="stagmenfashion-latest-grid__price-wrap">
                <span class="stagmenfashion-latest-grid__price">₹0.00</span>
              </div>
              <button type="button" class="stagmenfashion-latest-grid__quick-add-btn">
                Quick Add
              </button>
            </div>
          </div>
        {% endfor %}
      {% endif %}
    </div>
  </div>

  <!-- Wishlist Modal -->
  <div class="stagmenfashion-wishlist-modal" data-wishlist-modal style="display:none;" hidden>
    <div class="stagmenfashion-wishlist-modal__backdrop" data-wishlist-close></div>
    <div class="stagmenfashion-wishlist-modal__card">
      <button type="button" class="stagmenfashion-wishlist-modal__close" data-wishlist-close aria-label="Close">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>

      <div class="stagmenfashion-wishlist-modal__header">
        <div class="stagmenfashion-wishlist-modal__product-img" data-wishlist-img></div>
        <div class="stagmenfashion-wishlist-modal__product-info">
          <h3 class="stagmenfashion-wishlist-modal__title" data-wishlist-title></h3>
          <div class="stagmenfashion-wishlist-modal__price-wrap">
            <span class="stagmenfashion-wishlist-modal__price" data-wishlist-price></span>
            <span class="stagmenfashion-wishlist-modal__compare-price" data-wishlist-compare-price></span>
          </div>
        </div>
      </div>

      <div class="stagmenfashion-wishlist-modal__sizes">
        <div class="stagmenfashion-wishlist-modal__sizes-top">
          <span class="stagmenfashion-wishlist-modal__label" data-wishlist-size-label>Size</span>
        </div>
        <div class="stagmenfashion-wishlist-modal__size-grid" data-wishlist-sizes></div>
      </div>

      <button type="button" class="stagmenfashion-wishlist-modal__add-btn" data-wishlist-add>
        Add To List
      </button>
    </div>
  </div>

  <!-- Quick Add Modal -->
  <div class="stagmenfashion-qa-modal" data-qa-modal style="display:none;" hidden>
    <div class="stagmenfashion-qa-modal__backdrop" data-qa-close></div>
    <div class="stagmenfashion-qa-modal__card">
      <button type="button" class="stagmenfashion-qa-modal__close" data-qa-close aria-label="Close">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>

      <div class="stagmenfashion-qa-modal__header">
        <div class="stagmenfashion-qa-modal__product-img" data-qa-img></div>
        <div class="stagmenfashion-qa-modal__product-info">
          <h3 class="stagmenfashion-qa-modal__title" data-qa-title></h3>
          <span class="stagmenfashion-qa-modal__price" data-qa-price></span>
        </div>
      </div>

      <div class="stagmenfashion-qa-modal__sizes">
        <div class="stagmenfashion-qa-modal__sizes-top">
          <span class="stagmenfashion-qa-modal__label">Size</span>
        </div>
        <div class="stagmenfashion-qa-modal__size-grid" data-qa-sizes></div>
      </div>

      <div class="stagmenfashion-qa-modal__actions" data-qa-actions>
        <button type="button" class="stagmenfashion-qa-modal__btn stagmenfashion-qa-modal__btn--bag" data-qa-add-bag>Add To Bag</button>
        <button type="button" class="stagmenfashion-qa-modal__btn stagmenfashion-qa-modal__btn--buy" data-qa-buy-now>Buy Now</button>
      </div>
      <div class="stagmenfashion-qa-modal__sold-out" data-qa-sold-out style="display:none;">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="4.93" y1="4.93" x2="19.07" y2="19.07"></line>
        </svg>
        <span>Sold Out</span>
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const STORAGE_KEY = 'wishlist';
    let selectedSize = null;
    let currentProductId = '';

    const getWishlist = () => {
      try {
        const raw = localStorage.getItem(STORAGE_KEY);
        if (!raw) return [];
        const parsed = JSON.parse(raw);
        return Array.isArray(parsed) ? parsed : [];
      } catch(e) { return []; }
    };

    const saveWishlist = (list) => {
      try {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(list));
      } catch(e) {
        console.warn('Wishlist save failed:', e);
      }
    };

    const updateHeartStates = (scope) => {
      const wishlist = getWishlist();
      scope.querySelectorAll('[data-wishlist-heart]').forEach((btn) => {
        const handle = extractHandle(btn.dataset.productUrl);
        const isInList = wishlist.some((item) => item.handle === handle);
        btn.classList.toggle('is-saved', isInList);
        const svg = btn.querySelector('svg');
        if (svg) {
          svg.setAttribute('fill', isInList ? 'currentColor' : 'none');
        }
      });
    };

    const openModal = (btn) => {
      const modal = document.querySelector('[data-wishlist-modal]');
      if (!modal) return;
      const titleEl = modal.querySelector('[data-wishlist-title]');
      const priceEl = modal.querySelector('[data-wishlist-price]');
      const comparePriceEl = modal.querySelector('[data-wishlist-compare-price]');
      const imgEl = modal.querySelector('[data-wishlist-img]');
      const sizesEl = modal.querySelector('[data-wishlist-sizes]');
      const sizeLabel = modal.querySelector('[data-wishlist-size-label]');
      const addBtn = modal.querySelector('[data-wishlist-add]');

      const title = btn.dataset.productTitle || '';
      const price = btn.dataset.productPrice || '0';
      const comparePrice = btn.dataset.productComparePrice || '';
      const productId = String(btn.dataset.productId || '').trim();
      const productUrl = btn.dataset.productUrl || '#';
      const productImage = btn.dataset.productImage || '';
      const sizesRaw = btn.dataset.productSizes || '';
      const sizes = sizesRaw.split(',').map(s => s.trim()).filter(s => s.length > 0);

      currentProductId = productId;

      if (titleEl) titleEl.textContent = title;
      if (priceEl) priceEl.textContent = 'INR ' + price;

      if (comparePriceEl) {
        if (comparePrice && comparePrice !== price && comparePrice !== '0') {
          comparePriceEl.textContent = 'INR ' + comparePrice;
          comparePriceEl.style.display = 'inline';
        } else {
          comparePriceEl.textContent = '';
          comparePriceEl.style.display = 'none';
        }
      }

      if (imgEl) {
        if (productImage) {
          imgEl.innerHTML = '<img src="' + productImage + '" alt="' + title.replace(/"/g, '&quot;') + '" />';
          imgEl.style.display = 'flex';
        } else {
          imgEl.innerHTML = '';
          imgEl.style.display = 'none';
        }
      }

      selectedSize = null;

      if (sizesEl) {
        if (sizes.length > 0) {
          if (sizeLabel) sizeLabel.textContent = 'Size';
          sizesEl.parentElement.style.display = 'block';
          sizesEl.innerHTML = sizes.map((size, i) => {
            const isSelected = i === 0 ? ' is-selected' : '';
            if (i === 0) selectedSize = size;
            return '<button type="button" class="stagmenfashion-wishlist-modal__size' + isSelected + '" data-size-btn data-size="' + size + '">' + size + '</button>';
          }).join('');

          sizesEl.querySelectorAll('[data-size-btn]').forEach((sizeBtn) => {
            sizeBtn.addEventListener('click', () => {
              sizesEl.querySelectorAll('[data-size-btn]').forEach(b => b.classList.remove('is-selected'));
              sizeBtn.classList.add('is-selected');
              selectedSize = sizeBtn.dataset.size;
            });
          });
        } else {
          if (sizeLabel) sizeLabel.textContent = 'Default';
          sizesEl.parentElement.style.display = 'block';
          selectedSize = 'Default';
          sizesEl.innerHTML = '<button type="button" class="stagmenfashion-wishlist-modal__size is-selected" data-size-btn data-size="Default">Default</button>';
        }
      }

      if (addBtn) {
        addBtn.dataset.productId = productId;
        addBtn.dataset.productTitle = title;
        addBtn.dataset.productPrice = price;
        addBtn.dataset.productUrl = productUrl;
        addBtn.dataset.productImage = productImage;
      }

      modal.style.display = 'flex';
      modal.removeAttribute('hidden');
      requestAnimationFrame(() => {
        modal.classList.add('is-open');
      });
      document.body.style.overflow = 'hidden';
    };

    const closeModal = () => {
      const modal = document.querySelector('[data-wishlist-modal]');
      if (!modal) return;
      modal.classList.remove('is-open');
      setTimeout(() => {
        if (!modal.classList.contains('is-open')) {
          modal.style.display = 'none';
          modal.setAttribute('hidden', '');
        }
      }, 220);
      document.body.style.overflow = '';
    };

    const removeFromWishlist = (handle) => {
      let wishlist = getWishlist();
      wishlist = wishlist.filter((item) => item.handle !== handle);
      saveWishlist(wishlist);
      document.dispatchEvent(new CustomEvent('wishlist:update'));
    };

    const addToWishlist = (handle, id, title, price, image, size) => {
      const wishlist = getWishlist();
      const exists = wishlist.some((item) => item.handle === handle);
      if (!exists) {
        wishlist.push({ handle, id, title, price, image, size: size || '' });
        saveWishlist(wishlist);
        document.dispatchEvent(new CustomEvent('wishlist:update'));
      }
    };

    const extractHandle = (url) => {
      if (!url) return '';
      const parts = url.split('/');
      return parts[parts.length - 1] || parts[parts.length - 2] || '';
    };

    const toggleWishlist = (btn) => {
      const handle = extractHandle(btn.dataset.productUrl);
      const wishlist = getWishlist();
      const isInList = wishlist.some((item) => item.handle === handle);

      if (isInList) {
        removeFromWishlist(handle);
        updateHeartStates(document);
        return;
      }
      openModal(btn);
    };

    const initSection = (scope = document) => {
      scope.querySelectorAll('[data-wishlist-heart]').forEach((btn) => {
        if (btn.dataset.ready === 'true') return;
        btn.dataset.ready = 'true';
        btn.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          toggleWishlist(btn);
        });
      });

      // Track Arrow Navigation
      scope.querySelectorAll('[data-slider-track]').forEach((track) => {
        const parentSection = track.closest('.stagmenfashion-latest');
        if (!parentSection) return;
        const prevBtn = parentSection.querySelector('[data-slider-prev]');
        const nextBtn = parentSection.querySelector('[data-slider-next]');

        if (prevBtn && !prevBtn.dataset.ready) {
          prevBtn.dataset.ready = 'true';
          prevBtn.addEventListener('click', () => {
            const card = track.querySelector('.stagmenfashion-latest-grid__card');
            const cardWidth = card ? card.offsetWidth + 20 : 320;
            track.scrollBy({ left: -cardWidth, behavior: 'smooth' });
          });
        }

        if (nextBtn && !nextBtn.dataset.ready) {
          nextBtn.dataset.ready = 'true';
          nextBtn.addEventListener('click', () => {
            const card = track.querySelector('.stagmenfashion-latest-grid__card');
            const cardWidth = card ? card.offsetWidth + 20 : 320;
            track.scrollBy({ left: cardWidth, behavior: 'smooth' });
          });
        }
      });

      // Card size pills selection
      scope.querySelectorAll('[data-card-size-pills]').forEach((pillsContainer) => {
        const card = pillsContainer.closest('[data-product-card]');
        const qaBtn = card ? card.querySelector('[data-quick-add]') : null;

        pillsContainer.querySelectorAll('.stagmenfashion-latest-grid__size-pill').forEach((pill) => {
          if (pill.dataset.ready === 'true') return;
          pill.dataset.ready = 'true';
          pill.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            pillsContainer.querySelectorAll('.stagmenfashion-latest-grid__size-pill').forEach(p => p.classList.remove('is-selected'));
            pill.classList.add('is-selected');
            if (qaBtn) {
              qaBtn.dataset.qaSelectedSize = pill.dataset.sizeValue;
            }
          });
        });
      });

      updateHeartStates(scope);
    };

    const initModal = () => {
      document.querySelectorAll('[data-wishlist-close]').forEach((el) => {
        el.addEventListener('click', closeModal);
      });

      const addBtn = document.querySelector('[data-wishlist-add]');
      if (addBtn && !addBtn.dataset.ready) {
        addBtn.dataset.ready = 'true';
        addBtn.addEventListener('click', () => {
          const productId = currentProductId;
          const title = addBtn.dataset.productTitle || '';
          const price = addBtn.dataset.productPrice || '0';
          const url = addBtn.dataset.productUrl || '#';
          const image = addBtn.dataset.productImage || '';
          const handle = extractHandle(url);

          if (!handle) return;

          addToWishlist(handle, productId, title, price, image, selectedSize || '');
          closeModal();
          updateHeartStates(document);
        });
      }

      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeModal();
      });
    };

    /* ── Quick Add Modal & Action ───────────────────────────────── */
    let qaSelectedSize = null;
    let qaProductUrl = '';

    const getQAModal = () => document.querySelector('[data-qa-modal]');

    const resolveVariantId = (modal) => {
      const variantsData = modal?.dataset.qaVariants || '';
      const firstVariant = modal?.dataset.qaFirstVariant || '';
      let variantId = '';

      if (variantsData && qaSelectedSize) {
        const pairs = variantsData.split(',').filter(Boolean);
        for (const pair of pairs) {
          const [name, id] = pair.split(':');
          if (name && id && name.trim() === qaSelectedSize.trim()) {
            variantId = id.trim();
            break;
          }
        }
      }

      if (!variantId && firstVariant) {
        variantId = firstVariant;
      }
      return variantId;
    };

    const openQAModal = (btn) => {
      const modal = getQAModal();
      if (!modal) return;

      const titleEl = modal.querySelector('[data-qa-title]');
      const priceEl = modal.querySelector('[data-qa-price]');
      const imgEl = modal.querySelector('[data-qa-img]');
      const sizesEl = modal.querySelector('[data-qa-sizes]');
      const actionsEl = modal.querySelector('[data-qa-actions]');
      const soldOutEl = modal.querySelector('[data-qa-sold-out]');

      const title = btn.dataset.qaTitle || '';
      const price = btn.dataset.qaPrice || '0';
      qaProductUrl = btn.dataset.qaUrl || '';
      const image = btn.dataset.qaImage || '';
      const sizesRaw = btn.dataset.qaSizes || '';
      const firstVariant = btn.dataset.qaFirstVariant || '';
      const variantsData = btn.dataset.qaVariants || '';
      const isAvailable = btn.dataset.qaAvailable === 'true';
      const presetSize = btn.dataset.qaSelectedSize || null;

      modal.dataset.qaFirstVariant = firstVariant;
      modal.dataset.qaVariants = variantsData;

      if (titleEl) titleEl.textContent = title;
      if (priceEl) priceEl.textContent = 'RS. ' + price;

      if (imgEl) {
        if (image) {
          imgEl.innerHTML = '<img src="' + image + '" alt="' + title.replace(/"/g, '&quot;') + '" />';
          imgEl.style.display = 'block';
        } else {
          imgEl.innerHTML = '';
          imgEl.style.display = 'none';
        }
      }

      const sizes = sizesRaw.split(',').map(s => s.trim()).filter(Boolean);
      qaSelectedSize = presetSize || (sizes.length > 0 ? sizes[0] : null);

      if (sizesEl) {
        if (sizes.length > 0) {
          sizesEl.parentElement.style.display = 'block';
          sizesEl.innerHTML = sizes.map((sz) => {
            const isSel = (sz === qaSelectedSize) ? ' is-selected' : '';
            return '<button type="button" class="stagmenfashion-qa-modal__size' + isSel + '" data-qa-size-btn data-size="' + sz + '">' + sz + '</button>';
          }).join('');

          sizesEl.querySelectorAll('[data-qa-size-btn]').forEach((sb) => {
            sb.addEventListener('click', () => {
              sizesEl.querySelectorAll('[data-qa-size-btn]').forEach(b => b.classList.remove('is-selected'));
              sb.classList.add('is-selected');
              qaSelectedSize = sb.dataset.size;
            });
          });
        } else {
          sizesEl.parentElement.style.display = 'none';
        }
      }

      if (!isAvailable) {
        if (actionsEl) actionsEl.style.display = 'none';
        if (soldOutEl) soldOutEl.style.display = 'flex';
      } else {
        if (actionsEl) actionsEl.style.display = 'grid';
        if (soldOutEl) soldOutEl.style.display = 'none';
      }

      modal.style.display = 'flex';
      modal.removeAttribute('hidden');
      requestAnimationFrame(() => modal.classList.add('is-open'));
      document.body.style.overflow = 'hidden';
    };

    const closeQAModal = () => {
      const modal = getQAModal();
      if (!modal) return;
      modal.classList.remove('is-open');
      setTimeout(() => {
        if (!modal.classList.contains('is-open')) {
          modal.style.display = 'none';
          modal.setAttribute('hidden', '');
        }
      }, 220);
      document.body.style.overflow = '';
    };

    const addToCartAndRedirect = async (variantId, shouldRedirect = false) => {
      if (!variantId) return;

      try {
        const response = await fetch('/cart/add.js', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
          body: JSON.stringify({ items: [{ id: parseInt(variantId, 10), quantity: 1 }] }),
        });

        if (!response.ok) throw new Error('Failed to add item to cart');

        if (shouldRedirect) {
          window.location.href = '/checkout';
          return;
        }

        closeQAModal();

        // Dispatch theme cart updates
        document.dispatchEvent(new CustomEvent('cart:updated', { bubbles: true }));
        document.dispatchEvent(new CustomEvent('cart:refresh', { bubbles: true }));

        const cartDrawer = document.querySelector('cart-drawer') || document.querySelector('#CartDrawer');
        if (cartDrawer && typeof cartDrawer.open === 'function') {
          cartDrawer.open();
        } else {
          window.location.href = '/cart';
        }
      } catch (err) {
        console.error('Add to cart error:', err);
        if (qaProductUrl) window.location.href = qaProductUrl;
      }
    };

    const initQAModal = (scope = document) => {
      scope.querySelectorAll('[data-quick-add]').forEach((btn) => {
        if (btn.dataset.qaReady === 'true') return;
        btn.dataset.qaReady = 'true';
        btn.addEventListener('click', (e) => {
          e.preventDefault();
          e.stopPropagation();
          openQAModal(btn);
        });
      });

      const modal = getQAModal();
      if (modal && !modal.dataset.qaEventsBound) {
        modal.dataset.qaEventsBound = 'true';

        modal.querySelectorAll('[data-qa-close]').forEach((el) => {
          el.addEventListener('click', closeQAModal);
        });

        const bagBtn = modal.querySelector('[data-qa-add-bag]');
        if (bagBtn) {
          bagBtn.addEventListener('click', () => {
            const vId = resolveVariantId(modal);
            addToCartAndRedirect(vId, false);
          });
        }

        const buyBtn = modal.querySelector('[data-qa-buy-now]');
        if (buyBtn) {
          buyBtn.addEventListener('click', () => {
            const vId = resolveVariantId(modal);
            addToCartAndRedirect(vId, true);
          });
        }
      }
    };

    // Carousel init
    const initCarousels = (scope = document) => {
      scope.querySelectorAll('[data-carousel]').forEach((carousel) => {
        if (carousel.dataset.ready === 'true') return;
        carousel.dataset.ready = 'true';

        const slides = carousel.querySelectorAll('[data-carousel-slide]');
        const dots = carousel.querySelectorAll('[data-carousel-dot]');

        if (slides.length <= 1) return;

        let currentIndex = 0;

        const goTo = (index) => {
          if (index < 0) index = slides.length - 1;
          if (index >= slides.length) index = 0;
          slides[currentIndex].classList.remove('is-active');
          dots[currentIndex]?.classList.remove('is-active');
          currentIndex = index;
          slides[currentIndex].classList.add('is-active');
          dots[currentIndex]?.classList.add('is-active');
        };

        const cardEl = carousel.closest('[data-product-card]') || carousel;
        cardEl.addEventListener('mouseenter', () => {
          if (slides.length > 1) goTo(1);
        });

        cardEl.addEventListener('mouseleave', () => {
          if (slides.length > 1) goTo(0);
        });

        dots.forEach((dot) => {
          dot.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            goTo(parseInt(dot.dataset.index, 10));
          });
        });
      });
    };

    document.addEventListener('DOMContentLoaded', () => {
      initCarousels();
      initSection();
      initModal();
      initQAModal();
    });
    document.addEventListener('shopify:section:load', (event) => {
      initCarousels(event.target);
      initSection(event.target);
      initQAModal(event.target);
    });
  })();
</script>

<style>
  /* ══════════════════════════════════════════════════════════════
     SECTION CONTAINER & HEADER
     ══════════════════════════════════════════════════════════════ */
  .stagmenfashion-latest {
    background: var(--latest-bg, #ffffff);
    padding-top: clamp(12px, 1.8vw, 24px);
    padding-bottom: 8px;
  }

  .stagmenfashion-latest__shell {
    width: 100%;
    max-width: 100%;
    margin: 0;
    padding: 0 clamp(20px, 4vw, 56px);
    box-sizing: border-box;
  }

  .stagmenfashion-latest__head {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: clamp(18px, 2.5vw, 28px);
  }

  .stagmenfashion-latest__heading-wrap {
    flex: 1;
    min-width: 0;
  }

  .stagmenfashion-latest__heading {
    margin: 0;
    font-family: var(--font-heading--family, inherit);
    font-size: clamp(22px, 2.6vw, 32px);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--color-foreground-heading, #111111);
    text-transform: uppercase;
  }

  .stagmenfashion-latest__highlight {
    color: var(--color-foreground-heading, #111111);
  }

  .stagmenfashion-latest__subheading {
    margin: 6px 0 0;
    font-size: 14px;
    color: #666666;
    line-height: 1.4;
    font-weight: 400;
  }

  .stagmenfashion-latest__head-right {
    display: flex;
    align-items: center;
    gap: 16px;
    flex-shrink: 0;
  }

  .stagmenfashion-latest__discover-more {
    display: inline-flex;
    align-items: center;
    color: #111111;
    font-size: 13px;
    font-weight: 600;
    text-decoration: underline;
    text-underline-offset: 3px;
    transition: opacity 0.2s ease;
  }

  .stagmenfashion-latest__discover-more:hover {
    opacity: 0.7;
  }

  .stagmenfashion-latest__nav-arrows {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .stagmenfashion-latest__nav-arrow {
    width: 38px;
    height: 38px;
    border-radius: 50%;
    border: 1px solid #e0e0e0;
    background: #ffffff;
    color: #111111;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s ease;
    padding: 0;
  }

  .stagmenfashion-latest__nav-arrow:hover {
    border-color: #111111;
    background: #111111;
    color: #ffffff;
  }

  .stagmenfashion-latest__nav-arrow svg {
    width: 18px;
    height: 18px;
  }

  /* ══════════════════════════════════════════════════════════════
     PRODUCT SLIDER TRACK (3.2 CARDS DESKTOP)
     ══════════════════════════════════════════════════════════════ */
  .stagmenfashion-latest-grid {
    display: flex;
    gap: 20px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    padding-bottom: 8px;
  }

  .stagmenfashion-latest-grid::-webkit-scrollbar {
    display: none;
  }

  .stagmenfashion-latest-grid__card {
    flex: 0 0 calc(31.4% - 14px);
    min-width: 280px;
    scroll-snap-align: start;
    display: flex;
    flex-direction: column;
    background: #ffffff;
    border-radius: 16px;
    overflow: hidden;
    box-sizing: border-box;
  }

  @media (max-width: 989px) {
    .stagmenfashion-latest-grid__card {
      flex: 0 0 calc(46% - 10px);
      min-width: 240px;
    }
  }

  @media (max-width: 749px) {
    .stagmenfashion-latest-grid__card {
      flex: 0 0 calc(80% - 10px);
      min-width: 220px;
    }
  }

  /* ══════════════════════════════════════════════════════════════
     CARD MEDIA & BADGES
     ══════════════════════════════════════════════════════════════ */
  .stagmenfashion-latest-grid__media {
    position: relative;
    display: block;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    border-radius: 16px;
    background: #f5f5f5;
    text-decoration: none;
    touch-action: pan-y;
  }

  .stagmenfashion-latest-grid__image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0;
    z-index: 1;
    transition: opacity 0.3s ease;
  }

  .stagmenfashion-latest-grid__image.is-active {
    opacity: 1;
    z-index: 2;
  }

  .stagmenfashion-latest-grid__placeholder {
    width: 100%;
    height: 100%;
    object-fit: cover;
    background: #f0f0f0;
  }

  .stagmenfashion-card-badge {
    position: absolute;
    top: 12px;
    left: 12px;
    z-index: 5;
    background: rgba(17, 17, 17, 0.85);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    color: #ffffff;
    font-size: 11px;
    font-weight: 600;
    padding: 4px 10px;
    border-radius: 20px;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    line-height: 1;
  }

  /* Heart / Wishlist icon */
  .stagmenfashion-latest-grid__heart {
    position: absolute;
    top: 10px;
    right: 10px;
    z-index: 5;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    border: none;
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    border-radius: 50%;
    color: #111111;
    cursor: pointer;
    transition: all 0.2s ease;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .stagmenfashion-latest-grid__heart:hover {
    background: #ffffff;
    transform: scale(1.08);
  }

  .stagmenfashion-latest-grid__heart svg {
    width: 18px;
    height: 18px;
    stroke: currentColor;
    stroke-width: 1.8;
    fill: none;
  }

  .stagmenfashion-latest-grid__heart.is-saved svg {
    fill: #111111 !important;
  }

  /* Dots */
  .stagmenfashion-latest-grid__dots {
    position: absolute;
    bottom: 10px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 5;
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 3px 6px;
    background: rgba(0, 0, 0, 0.35);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    border-radius: 999px;
  }

  .stagmenfashion-latest-grid__dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.5);
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .stagmenfashion-latest-grid__dot.is-active {
    background: #ffffff;
    width: 10px;
    border-radius: 999px;
  }

  /* ══════════════════════════════════════════════════════════════
     CARD DETAILS & QUICK ADD BUTTON
     ══════════════════════════════════════════════════════════════ */
  .stagmenfashion-latest-grid__info {
    padding: 14px 4px 4px 4px;
    display: flex;
    flex-direction: column;
    gap: 4px;
    flex: 1;
  }

  .stagmenfashion-latest-grid__title-link {
    text-decoration: none;
    color: inherit;
  }

  .stagmenfashion-latest-grid__title {
    margin: 0;
    font-family: var(--font-heading--family, inherit);
    font-size: 14px;
    font-weight: 700;
    color: #111111;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    line-height: 1.25;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .stagmenfashion-latest-grid__subtitle {
    margin: 0;
    font-size: 11px;
    color: #888888;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .stagmenfashion-latest-grid__price-wrap {
    margin-top: 2px;
  }

  .stagmenfashion-latest-grid__price {
    font-size: 14px;
    font-weight: 700;
    color: #111111;
  }

  .stagmenfashion-latest-grid__size-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 6px;
    margin-bottom: 8px;
  }

  .stagmenfashion-latest-grid__size-pill {
    padding: 4px 10px;
    border-radius: 6px;
    border: 1px solid #e2e2e2;
    background: #ffffff;
    color: #333333;
    font-size: 11px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .stagmenfashion-latest-grid__size-pill:hover,
  .stagmenfashion-latest-grid__size-pill.is-selected {
    border-color: #111111;
    background: #111111;
    color: #ffffff;
  }

  .stagmenfashion-latest-grid__quick-add-btn {
    width: 100%;
    padding: 11px 16px;
    border-radius: 8px;
    border: none;
    background: #111111;
    color: #ffffff;
    font-size: 13px;
    font-weight: 600;
    text-transform: none;
    letter-spacing: 0.02em;
    cursor: pointer;
    transition: background 0.2s ease, transform 0.15s ease;
    margin-top: auto;
  }

  .stagmenfashion-latest-grid__quick-add-btn:hover {
    background: #000000;
  }

  .stagmenfashion-latest-grid__quick-add-btn:active {
    transform: scale(0.98);
  }

  /* ══════════════════════════════════════════════════════════════
     MODALS
     ══════════════════════════════════════════════════════════════ */
  .stagmenfashion-wishlist-modal,
  .stagmenfashion-qa-modal {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 9999;
    align-items: center;
    justify-content: center;
  }

  .stagmenfashion-wishlist-modal.is-open,
  .stagmenfashion-qa-modal.is-open {
    display: flex;
  }

  .stagmenfashion-wishlist-modal__backdrop,
  .stagmenfashion-qa-modal__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    cursor: pointer;
  }

  .stagmenfashion-wishlist-modal__card,
  .stagmenfashion-qa-modal__card {
    position: relative;
    z-index: 1;
    width: 90%;
    max-width: 380px;
    background: #ffffff;
    border-radius: 12px;
    padding: 20px;
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.18);
  }

  .stagmenfashion-wishlist-modal__close,
  .stagmenfashion-qa-modal__close {
    position: absolute;
    top: 12px;
    right: 12px;
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    border: none;
    background: transparent;
    color: #666666;
    cursor: pointer;
    border-radius: 50%;
  }

  .stagmenfashion-wishlist-modal__header,
  .stagmenfashion-qa-modal__header {
    display: flex;
    gap: 12px;
    align-items: center;
    margin-bottom: 16px;
  }

  .stagmenfashion-wishlist-modal__product-img,
  .stagmenfashion-qa-modal__product-img {
    width: 56px;
    height: 56px;
    border-radius: 8px;
    overflow: hidden;
    flex-shrink: 0;
    background: #f5f5f5;
  }

  .stagmenfashion-wishlist-modal__product-img img,
  .stagmenfashion-qa-modal__product-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .stagmenfashion-wishlist-modal__title,
  .stagmenfashion-qa-modal__title {
    margin: 0 0 4px 0;
    font-size: 14px;
    font-weight: 700;
    color: #111111;
  }

  .stagmenfashion-wishlist-modal__price,
  .stagmenfashion-qa-modal__price {
    font-size: 13px;
    font-weight: 600;
    color: #666666;
  }

  .stagmenfashion-wishlist-modal__size-grid,
  .stagmenfashion-qa-modal__size-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 8px;
    margin-bottom: 16px;
  }

  .stagmenfashion-wishlist-modal__size,
  .stagmenfashion-qa-modal__size {
    padding: 6px 14px;
    border-radius: 6px;
    border: 1px solid #e2e2e2;
    background: #ffffff;
    color: #111111;
    font-size: 12px;
    font-weight: 500;
    cursor: pointer;
  }

  .stagmenfashion-wishlist-modal__size.is-selected,
  .stagmenfashion-qa-modal__size.is-selected {
    background: #111111;
    color: #ffffff;
    border-color: #111111;
  }

  .stagmenfashion-wishlist-modal__add-btn {
    width: 100%;
    padding: 12px;
    border-radius: 8px;
    border: none;
    background: #111111;
    color: #ffffff;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    cursor: pointer;
  }

  .stagmenfashion-qa-modal__actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }

  .stagmenfashion-qa-modal__btn {
    padding: 12px;
    border-radius: 8px;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    cursor: pointer;
    border: none;
  }

  .stagmenfashion-qa-modal__btn--bag {
    background: #ffffff;
    color: #111111;
    border: 1px solid #111111;
  }

  .stagmenfashion-qa-modal__btn--buy {
    background: #111111;
    color: #ffffff;
  }

  .stagmenfashion-qa-modal__sold-out {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 12px;
    background: #f5f5f5;
    color: #888888;
    border-radius: 8px;
    font-size: 13px;
    font-weight: 600;
  }
</style>

{% schema %}
{
  "name": "Latest Items Scroll",
  "tag": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "DISCOVER OUR BESTSELLERS"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Explore our curated fragrance collection"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection",
      "info": "Products shown from this collection."
    },
    {
      "type": "text",
      "id": "discover_more_label",
      "label": "Discover More Label",
      "default": "Discover more"
    },
    {
      "type": "url",
      "id": "discover_more_link",
      "label": "Discover More Link (Optional)"
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    }
  ],
  "presets": [
    {
      "name": "New Arrivals Scroll"
    }
  ]
}
{% endschema %}
